# Open WebUI — its own venv (tools/venv-webui). Conflicts with aider's pins, so kept separate.
# Lock after a good install: tools\venv-webui\Scripts\pip freeze > tools\webui-requirements.lock, then mark
# Windows-only rows (pywin32, win32_setctime) with ' ; sys_platform == "win32"': the lock installs on every OS.
open-webui
